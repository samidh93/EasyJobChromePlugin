{
  "name": "easyjob-api-tests",
  "version": "1.0.0",
  "description": "Integration tests for EasyJob API",
  "main": "index.js",
  "scripts": {
    "test": "mocha --timeout 30000 --recursive *.test.js",
    "test:watch": "mocha --timeout 30000 --recursive *.test.js --watch",
    "test:verbose": "mocha --timeout 30000 --recursive *.test.js --reporter spec"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "chai": "^4.3.10",
    "mocha": "^10.2.0"
  },
  "devDependencies": {},
  "keywords": ["api", "testing", "integration", "easyjob"],
  "author": "EasyJob Team",
  "license": "MIT"
} 